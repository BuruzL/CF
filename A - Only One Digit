#include<stdio.h>
int main(){
  int n;
  scanf("%d",&n);
  while(n--){
    int num;
    scanf("%d", &num);
    int t=num;
    int out=t%10;
    while(t){
      if(out>(t%10)){
        out=t%10;
      }
      t=t/10;
    }
    printf("%d\n", out);
  }
  return 0;
}
