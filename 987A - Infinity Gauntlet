#include <stdio.h>
#include <limits.h>
int main()
{
    int n;
    scanf("%d", &n);
    char out[6 - n];
    int chk[6];
    for (int i = 0; i < 6; i++)
        chk[i] = 0;
    for (int i = 0; i < n; i++)
    {
        char col[10];
        scanf("%s", col);
        if (col[0] == 'r')
            chk[4] = 1;
        if (col[0] == 'y')
            chk[5] = 1;
        if (col[0] == 'p')
            chk[0] = 1;
        if (col[0] == 'g')
            chk[1] = 1;
        if (col[0] == 'b')
            chk[2] = 1;
        if (col[0] == 'o')
            chk[3] = 1;
    }
    printf("%d\n", (6 - n));
    if (chk[0] == 0)
        printf("Power\n");
    if (chk[1] == 0)
        printf("Time\n");
    if (chk[2] == 0)
        printf("Space\n");
    if (chk[3] == 0)
        printf("Soul\n");
    if (chk[4] == 0)
        printf("Reality\n");
    if (chk[5] == 0)
        printf("Mind\n");

    return 0;
}
