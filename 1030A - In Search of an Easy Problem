#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);
    int q[n];
    int chk=0;
   for(int i=0;i<n; i++){
    scanf("%d", &q[i]);
    if(q[i]==1){
        chk=1;
    }
   }
   if(chk==1)puts("HARD");
   else puts("EASY");
    return 0;
}
