#include<stdio.h>
int main(){
    int n;
    scanf("%d", &n);
    int num[n];
    for(int i=0; i<n; i++){
        scanf("%d", &num[i]);
    }
    int new[n];
for(int j=0; j<n-1; j++){
    for(int i=0; i<n-1-j; i++){
        if(num[i]>num[i+1]){
            int temp;
            temp=num[i];
            num[i]=num[i+1];
            num[i+1]=temp;
        }
    }
}
    for(int i=0; i<n; i++){
        printf("%d ", num[i]);
    }
    return 0;
}
