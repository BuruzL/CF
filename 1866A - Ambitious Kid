#include<stdio.h>
#include<stdlib.h>
#include<limits.h>
int main(){
    int n;
    scanf("%d", &n);
    int num[n];
    int min=INT_MAX;
    for(int i=0; i<n; i++){
        scanf("%d", &num[i]);
        if(abs(num[i])<min)min=abs(num[i]);
    }
    printf("%d", min);
    return 0;
}
