#include<stdio.h>
#include<string.h>
int main(){
    char p[101];
    scanf("%100s", p);
    int len;
    len=strlen(p);
    int chk=0;
    for(int i=0; i<len; i++){
        if(p[i]=='H' || p[i]=='Q' || p[i]=='9'){
            chk=1;
        }
    }
    if(chk==1)printf("YES");
    else printf("NO");
    return 0;
}
