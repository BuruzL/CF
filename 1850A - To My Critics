#include<stdio.h>
int main(){
    int n;
    scanf("%d", &n);
    int found[n];
    for(int i=0; i<n; i++)found[i]=0;
    for(int i=0; i<n; i++){
        int num[3];
        for(int j=0; j<3; j++){
            scanf("%d", &num[j]);
        }
        if(num[0]+num[1]>=10){
                found[i]=1;
        }else if(num[0]+num[2]>=10){
                found[i]=1;
        }else if(num[2]+num[1]>=10){
                found[i]=1;
        }
    }
    for(int i=0; i<n; i++){
        if(found[i]==1)printf("YES\n");
        else printf("NO\n");
    }
    
    return 0;
}
