#include<stdio.h>
int main(){
    int n;
    scanf("%d",&n);
    while(n--){
        int a,b;
        scanf("%d %d", &a, &b);
        int chk=0;
        if(a==b)chk=0;
        else if(a%b==0 || b%a==0)chk=1;
        else chk=2;

        printf("%d\n", chk);
    }
    return 0;
}
